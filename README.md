# test parcel

## Development

Install NPM package

```zsh
$ npm i
$ sudo npm install copyfiles -g
$ sudo npm install replace -g
$ npm install parcel-bundler -g
```

Development

```zsh
$ npm start
```

